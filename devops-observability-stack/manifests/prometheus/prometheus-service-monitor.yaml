apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sample-app-monitor
  labels:
    release: kube-prometheus
spec:
  selector:
    matchLabels:
      app: sample-app
  endpoints:
    - port: http
      interval: 15s
  namespaceSelector:
    matchNames:
      - default